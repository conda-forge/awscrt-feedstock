schema_version: 1

context:
  version: "0.31.0"

package:
  name: awscrt
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/a/awscrt/awscrt-${{ version }}.tar.gz
  sha256: dec62174f0b9be2c326eeda062e744ebbfaa8b6ec7287f52687b7ad430115bbf
  patches:
    - 0001-Don-t-force-static-linkage.patch

build:
  number: 0

requirements:
  build:
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
    - if: win
      then: nasm
  host:
    - python
    - pip
    - setuptools
    - if: linux
      then: s2n
    - aws-checksums
    - aws-c-common
    - aws-c-cal
    - aws-c-io
    - aws-c-event-stream
    - aws-c-http
    - aws-c-auth
    - aws-c-mqtt
    - aws-c-s3
  run:
    - python

tests:
  - python:
      imports:
        - awscrt
        - awscrt.eventstream
  - requirements:
      run:
        - pip
    script:
      - pip check

about:
  summary: A common runtime for AWS Python projects
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/awslabs/aws-crt-python

extra:
  recipe-maintainers:
    - ocefpaf
    - xhochy
